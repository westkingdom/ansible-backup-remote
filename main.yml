---
- name: wk backups
  hosts: westkingdom
  user: root
  become: true
  become_user: maint
  vars_files:
    - vars.yml

  roles:
    - { role: ../roles/backupdbs, tags: [ 'dbs' ] }
    - { role: ../roles/backupfiles, tags: [ 'files' ] }
    - { role: ../roles/notify, tags: [ 'notify' ] }